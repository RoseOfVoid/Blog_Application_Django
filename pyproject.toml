[tool.poetry]
name = "blog-application-django"
version = "0.1.0"
description = ""
authors = ["Work_PC <bejr.dominik@seznam.cz>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
python-dotenv = "^1.0.1"
django = ">=5.0.4,<5.1.0"
django-taggit = "^6.1.0"
markdown = "^3.7"
psycopg = {extras = ["binary"], version = "^3.2.5"}
chardet = "^5.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
